version: '3'
services:
  pigeon-nginx:
    image: pigeon-nginx:latest
    build: ./pigeon-nginx
    restart: always
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./pigeon-nginx/data/certbot/conf:/etc/letsencrypt
      - ./pigeon-nginx/data/certbot/www:/var/www/certbot
    command: "/bin/sh -c 'while :; do sleep 6h & wait $${!}; nginx -s reload; done & nginx -g \"daemon off;\"'"
  pigeon-certbot:
    image: certbot/certbot
    restart: always
    volumes:
      - ./pigeon-nginx/data/certbot/conf:/etc/letsencrypt
      - ./pigeon-nginx/data/certbot/www:/var/www/certbot
    entrypoint: "/bin/sh -c 'trap exit TERM; while :; do certbot renew; sleep 12h & wait $${!}; done;'"